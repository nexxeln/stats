---
type Props = {
  name: string;
  value: number;
};

const { name, value } = Astro.props;
---

<div class="flex flex-col bg-dark-900 rounded-xl p-4">
  <h3 class="text-xl font-bold">{name}</h3>
  <span class="text-2xl font-bold">{value}</span>
</div>
